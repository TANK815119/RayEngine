
/**
 * stupid place to store code
 */
public class CODESTOW
{
    //shader method(very not working)
            // int textureId = renderPipe.createTexture(renderPipe.render(camera), camera.getPixelWidth(), camera.getPixelHeight());

            // GL11.glBindTexture(GL11.GL_TEXTURE_2D, textureId);

            // GL11.glBegin(GL11.GL_QUADS);
            // {
                // GL11.glTexCoord2f(0, 0);
                // GL11.glVertex2f(-1.0f, -1.0f);

                // GL11.glTexCoord2f(1, 0);
                // GL11.glVertex2f(1.0f, -1.0f);

                // GL11.glTexCoord2f(1, 1);
                // GL11.glVertex2f(1.0f, 1.0f);

                // GL11.glTexCoord2f(0, 1);
                // GL11.glVertex2f(-1.0f, 1.0f);
            // }
            // GL11.glEnd();
            // GL11.glBindTexture(GL11.GL_TEXTURE_2D, 0);

            // float height = camera.getPixelHeight();
            // float width = camera.getPixelWidth();
            // GL11.glBegin(GL11.GL_POINTS);

            // for (float y = -1; y < 1; y+=(2/height)) {
                // for (float x = -1; x < 1; x+=(2/width)) {
                    // GL11.glColor3f((float)Math.random(), (float)Math.random(), (float)Math.random());
                    // GL11.glVertex2f(x, y);
                // }
            // }
            // GL11.glEnd();
}
